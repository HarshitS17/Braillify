# Runtime dependencies ONLY.
# This file is installed into the Vercel Python function bundle and the Docker
# image — every package here must be imported by backend/app at runtime.
# Dev/test tooling lives in requirements-dev.txt.

fastapi>=0.115.0
uvicorn>=0.30.0
pydantic>=2.9.0
pydantic-settings>=2.5.0
opencv-python-headless>=4.10.0
numpy>=1.26.0
Pillow>=10.4.0
svgwrite>=1.4.3
python-multipart>=0.0.9
PyMuPDF>=1.24.0
reportlab>=4.2.0
vercel>=0.10.0

