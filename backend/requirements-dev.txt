# Dev/test-only dependencies. NOT installed into the Vercel function bundle
# or the Docker runtime image (keeps the deployed bundle under the 500 MB limit).
pytest>=8.3.0
pytest-asyncio>=0.24.0
httpx>=0.27.0
ruff>=0.6.0
mypy>=1.11.0
